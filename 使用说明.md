# Frieren 桌宠使用说明

一只住在 **DeepSeek Harness 网页版**（`dsh --profile web`）里的 Q 版芙莉莲桌宠。
她会实时反映 Agent 的工作状态，陪你写代码。

## 一、安装

```sh
dsh plugin --profile web add github:dakeshui123/dsh-pet-frieren
```

然后启动网页版：

```sh
dsh --profile web
```

更新 / 卸载：

```sh
dsh plugin --profile web update dsh-pet-frieren   # 更新
dsh plugin --profile web remove dsh-pet-frieren   # 卸载
```

## 二、桌宠会跟着 Agent 状态变

| 桌宠表现 | Agent 状态 |
| --- | --- |
| 发呆循环 | 空闲（无任务、回合之间） |
| 等待姿势 | 思考中（模型正在推理，还没开始动手） |
| 审阅姿势 | 工具调用执行中（正在跑命令、读写文件等） |
| 小跑 | 流式输出中（正式输出回答内容） |
| 等待姿势 | 等待工具审批 / 等你回答问题 |
| 审阅姿势 | 计划评审（plan mode 提交计划等待确认） |
| 扑倒定格 | 报错了（定格到错误清除才爬起来） |
| 切换会话 | 桌宠立即切换成新会话的状态 |

状态每次切换，她还会冒出一句台词（约 3.5 秒后淡出）。空闲时她也不闲着：
每 12~30 秒自己挥挥手，每 45~90 秒偶尔跳一下，每 20~40 秒闲聊一句。
Agent 忙碌时她会安静地反映状态，不会乱动。

## 三、怎么和她玩

| 操作 | 效果 |
| --- | --- |
| 单击 | 挥手一次 |
| 快速双击 | 跳一下 |
| 按住拖拽 | 移动位置（左右拖会朝对应方向跑） |
| 悬停 + 点 ✕ | 隐藏桌宠 |
| 点原地的小徽章 | 恢复显示 |
| 拖拽小徽章 | 移动桌宠的位置（恢复后她会在徽章所在处出现） |

位置和隐藏状态都会被记住，刷新页面后还在原地。

## 四、常见问题

**Q：桌宠没出现？**
确认已安装到 web profile：`dsh plugin --profile web list` 里应有
`dsh-pet-frieren`，然后重启 `dsh --profile web`。

**Q：桌宠不见了？**
可能是不小心点了 ✕。找一下屏幕角落的半透明小徽章，点它恢复。

**Q：桌宠不动了？**
如果系统开启了"减少动态效果"（辅助功能 → 显示 → 减少动态效果），
桌宠会刻意保持静止（只显示当前状态的静态帧）。关掉该设置即恢复动画。

**Q：想看桌宠切换状态，怎么触发？**
发一条消息让 Agent 干活 → 小跑；触发工具审批 → 等待；
在 plan mode 下让 Agent 提交计划 → 审阅；故意制造报错 → 扑倒。

## 五、反馈

有问题或建议欢迎在 GitHub 仓库提 issue：
<https://github.com/dakeshui123/dsh-pet-frieren>
